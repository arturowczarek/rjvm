type Field = ();
type Method = ();
type MethodCode = ();

pub struct Class {
    constant_pool: (),
    fields: Vec<Field>,
    methods: Vec<Method>,
    methods_code: Vec<MethodCode>,
}

impl Class {

}
